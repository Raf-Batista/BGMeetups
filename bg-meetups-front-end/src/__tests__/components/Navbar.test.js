import React from 'react';
import { shallow } from 'enzyme';
import Navbar from '../../components/Navbar';

test('has the correct links in navbar',() => {
    const wrapper = shallow(<Navbar />)
    
});